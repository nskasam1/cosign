import GroupSession from "./GroupSession";

// Same placeholder as GroupSession until Phase 5B builds the real
// needs-intersection flow.
const GroupSessionNew = () => <GroupSession />;

export default GroupSessionNew;
